import { CartDetails } from '@components'

const Cart = () => {
	return (
		<main className='flex min-h-screen flex-col items-center justify-between py-8 relative'>
			<CartDetails />
		</main>
	)
}

export default Cart
